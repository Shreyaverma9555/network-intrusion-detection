# Data

Place training datasets, live packet CSV exports, and `.pcap`/`.pcapng` files in
this folder.

Expected training target column: `label`.

Large packet captures and datasets are intentionally not committed.
